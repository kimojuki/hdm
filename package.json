{
  "name": "hdm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "assets:manifest": "node scripts/build-asset-manifest.mjs",
    "dev": "npm run assets:manifest && vite",
    "build": "npm run assets:manifest && vite build",
    "start": "node server.mjs",
    "start:infomaniak": "bash scripts/start.sh",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.172.0",
    "three-mesh-bvh": "^0.9.10"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "vite": "^6.0.7"
  }
}
